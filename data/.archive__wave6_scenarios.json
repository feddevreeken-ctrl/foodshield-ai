{"scenarios":[
"{id:\"grapes_chile_drought\", label:\"Chile central-valley drought hits table grapes\", cat:\"Grape sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Chile is the Southern Hemisphere's largest table-grape exporter; a deepening central-valley drought and reduced Aconcagua/Maipo irrigation cuts export volumes, squeezing US, Chinese and Dutch off-season buyers (USDA FAS, ODEPA).\",\n    formula:\"Δ = (shocked-exporter share of this country's grapes imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"CHL\", _src:\"USDA FAS / ODEPA Chile\", _commodity:\"grapes\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"CHL\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"grapes_peru_elnino\", label:\"El Niño disrupts Peru's Ica/Piura grape harvest\", cat:\"Grape sector\", baseSev:0.28, sevUnit:'×', channel:0,\n    desc:\"Peru has become a leading table-grape exporter from Ica and Piura; a strong El Niño brings heat and flooding that delays and shrinks the crop, hitting US, EU and UK importers in the early window (USDA FAS, Provid Peru).\",\n    formula:\"Δ = (shocked-exporter share of this country's grapes imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"PER\", _src:\"USDA FAS / PROVID Peru\", _commodity:\"grapes\",\n    kick: function(c, sv=0.28) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"PER\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.28){ return Math.round(this.kick(c, sv)); }}",

"{id:\"grapes_spain_drought\", label:\"Spain drought cuts Mediterranean table-fruit supply\", cat:\"Grape sector\", baseSev:0.27, sevUnit:'×', channel:0,\n    desc:\"Spain is a core EU supplier of table grapes and other table fruit; prolonged Andalusian and Murcian drought with irrigation restrictions reduces volumes, tightening supply for Germany, France and other intra-EU buyers (FAO, Eurostat/FEPEX).\",\n    formula:\"Δ = (shocked-exporter share of this country's grapes imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"ESP\", _src:\"FAO / FEPEX Spain\", _commodity:\"grapes\",\n    kick: function(c, sv=0.27) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"ESP\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.27){ return Math.round(this.kick(c, sv)); }}",

"{id:\"grapes_zaf_capeports\", label:\"Cape Town port congestion strands South African grapes\", cat:\"Grape sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"South Africa is a major Southern Hemisphere table-grape exporter shipping mostly through Cape Town; equipment failures and severe port congestion strand reefer containers, delaying deliveries to the EU and UK during peak season (Transnet, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's grapes imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"ZAF\", _src:\"USDA FAS / Transnet\", _commodity:\"grapes\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"ZAF\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Grapes & table fruit\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"spices_india_export_ban\", label:\"India tightens spice export policy after monsoon shortfall\", cat:\"Spice sector\", baseSev:0.32, sevUnit:'×', channel:0,\n    desc:\"India is the world's largest spice exporter; a weak monsoon and food-inflation politics prompt export curbs and quality holds, hitting US, EU and Middle East buyers reliant on Indian chilli, turmeric and cumin (Spices Board India, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's spices imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"IND\", _src:\"Spices Board India / USDA FAS\", _commodity:\"spices\",\n    kick: function(c, sv=0.32) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"IND\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.32){ return Math.round(this.kick(c, sv)); }}",

"{id:\"spices_vietnam_pepper_drought\", label:\"Drought slashes Vietnam's black-pepper crop\", cat:\"Spice sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Vietnam supplies a large share of the world's black pepper from the Central Highlands; drought and falling planted area cut output, tightening supply for US, EU and Indian processors (Vietnam Pepper Association, IPC).\",\n    formula:\"Δ = (shocked-exporter share of this country's spices imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"VNM\", _src:\"International Pepper Community (IPC) / VPA\", _commodity:\"spices\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"VNM\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"spices_madagascar_vanilla_cyclone\", label:\"Cyclone devastates Madagascar's vanilla belt\", cat:\"Spice sector\", baseSev:0.33, sevUnit:'×', channel:0,\n    desc:\"Madagascar produces most of the world's natural vanilla in the SAVA region; a major cyclone flattens vines just before harvest, spiking prices and squeezing US and EU flavour-and-fragrance buyers (FAO, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's spices imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"MDG\", _src:\"FAO / USDA FAS\", _commodity:\"spices\",\n    kick: function(c, sv=0.33) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"MDG\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.33){ return Math.round(this.kick(c, sv)); }}",

"{id:\"spices_sri_lanka_cinnamon\", label:\"Sri Lanka cinnamon supply hit by weather and FX crisis\", cat:\"Spice sector\", baseSev:0.28, sevUnit:'×', channel:0,\n    desc:\"Sri Lanka dominates true (Ceylon) cinnamon exports; erratic monsoon weather plus input and foreign-exchange shortages disrupt processing and shipments to US, EU and Mexican buyers (Sri Lanka Export Development Board, ITC).\",\n    formula:\"Δ = (shocked-exporter share of this country's spices imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"LKA\", _src:\"ITC Trade Map / Sri Lanka EDB\", _commodity:\"spices\",\n    kick: function(c, sv=0.28) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"LKA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Spices\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.28){ return Math.round(this.kick(c, sv)); }}",

"{id:\"frozenpotato_belgium_drought\", label:\"NW-Europe drought shrinks Belgium's processing-potato crop\", cat:\"Frozen-potato sector\", baseSev:0.32, sevUnit:'×', channel:0,\n    desc:\"Belgium is the world's largest frozen-potato (fries) exporter; a hot, dry NW-European summer cuts processing-potato yields and tightens raw supply, raising prices for importers across the EU, Middle East and Asia (Belgapom, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's frozenpotato imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"BEL\", _src:\"Belgapom / USDA FAS\", _commodity:\"frozenpotato\",\n    kick: function(c, sv=0.32) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"BEL\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.32){ return Math.round(this.kick(c, sv)); }}",

"{id:\"frozenpotato_netherlands_floods\", label:\"Wet harvest disrupts Dutch frozen-fries output\", cat:\"Frozen-potato sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"The Netherlands is a top frozen-potato exporter; an exceptionally wet autumn waterlogs fields and strands the harvest, curbing factory throughput and shipments to EU, African and Asian buyers (Wageningen/VAVI, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's frozenpotato imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"NLD\", _src:\"VAVI Netherlands / USDA FAS\", _commodity:\"frozenpotato\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"NLD\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"frozenpotato_canada_cropfail\", label:\"Prairie crop failure cuts Canadian frozen-potato exports\", cat:\"Frozen-potato sector\", baseSev:0.29, sevUnit:'×', channel:0,\n    desc:\"Canada is a major frozen-potato exporter from Manitoba and Alberta processing zones; drought or early frost reduces the crop, tightening supply for the US, Japan and other Pacific buyers (Statistics Canada, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's frozenpotato imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"CAN\", _src:\"Statistics Canada / USDA FAS\", _commodity:\"frozenpotato\",\n    kick: function(c, sv=0.29) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"CAN\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.29){ return Math.round(this.kick(c, sv)); }}",

"{id:\"frozenpotato_usa_pnw\", label:\"Pacific NW heat dome dents US frozen-potato supply\", cat:\"Frozen-potato sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"The US is a leading frozen-potato exporter from Idaho, Washington and Oregon; a Pacific Northwest heat dome lowers yields and fry quality, tightening export availability to Japan, Mexico and other Asian buyers (USDA NASS, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's frozenpotato imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"USA\", _src:\"USDA NASS / USDA FAS\", _commodity:\"frozenpotato\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"USA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Frozen potato\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"liveanimals_australia_export_ban\", label:\"Australia suspends live-export over welfare/disease\", cat:\"Live-animal sector\", baseSev:0.33, sevUnit:'×', channel:0,\n    desc:\"Australia is a leading live cattle and sheep exporter; a welfare-triggered suspension or biosecurity disease scare halts shipments, cutting off Indonesia, Vietnam and Middle East importers reliant on Australian feeder and slaughter stock (Meat & Livestock Australia, DAFF).\",\n    formula:\"Δ = (shocked-exporter share of this country's liveanimals imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"AUS\", _src:\"Meat & Livestock Australia (MLA) / DAFF\", _commodity:\"liveanimals\",\n    kick: function(c, sv=0.33) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"AUS\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.33){ return Math.round(this.kick(c, sv)); }}",

"{id:\"liveanimals_france_bluetongue\", label:\"Bluetongue outbreak halts French feeder-cattle exports\", cat:\"Live-animal sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"France is the EU's largest exporter of live feeder cattle; a bluetongue (BTV) outbreak triggers movement restrictions and import bans on French stock, disrupting Italy, Spain and Algeria's fattening and slaughter supply (French Ministry of Agriculture, WOAH).\",\n    formula:\"Δ = (shocked-exporter share of this country's liveanimals imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"FRA\", _src:\"French Ministry of Agriculture / WOAH\", _commodity:\"liveanimals\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"FRA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"liveanimals_denmark_asf\", label:\"ASF detection halts Danish live-pig movements\", cat:\"Live-animal sector\", baseSev:0.32, sevUnit:'×', channel:0,\n    desc:\"Denmark is a major exporter of live weaner and feeder pigs; an African swine fever (ASF) detection triggers movement standstills and third-country bans, cutting Germany, Poland and Italy off from Danish piglet supply (Danish Agriculture & Food Council, WOAH).\",\n    formula:\"Δ = (shocked-exporter share of this country's liveanimals imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"DNK\", _src:\"Danish Agriculture & Food Council / WOAH\", _commodity:\"liveanimals\",\n    kick: function(c, sv=0.32) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"DNK\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.32){ return Math.round(this.kick(c, sv)); }}",

"{id:\"liveanimals_germany_fmd\", label:\"Foot-and-mouth scare freezes German live-animal trade\", cat:\"Live-animal sector\", baseSev:0.31, sevUnit:'×', channel:0,\n    desc:\"Germany is a significant EU exporter of live cattle and pigs; a foot-and-mouth disease (FMD) case triggers an immediate export freeze and partner-country bans, disrupting buyers across the EU and neighbouring third countries (German BMEL / FLI, WOAH).\",\n    formula:\"Δ = (shocked-exporter share of this country's liveanimals imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"DEU\", _src:\"German BMEL / Friedrich-Loeffler-Institut / WOAH\", _commodity:\"liveanimals\",\n    kick: function(c, sv=0.31) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"DEU\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Live animals\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.31){ return Math.round(this.kick(c, sv)); }}"
],
 "channels":[
  {"sector":"Grape sector","dot":"#7b2d52","blurb":"Cited bilateral table-fruit-trade shocks — impact from real flow dependence."},
  {"sector":"Spice sector","dot":"#c8731f","blurb":"Cited bilateral spice-trade shocks — impact from real flow dependence."},
  {"sector":"Frozen-potato sector","dot":"#d9a441","blurb":"Cited bilateral frozen-potato-trade shocks — impact from real flow dependence."},
  {"sector":"Live-animal sector","dot":"#8a5a2b","blurb":"Cited bilateral live-animal-trade shocks — impact from real flow dependence."}
 ]
}
