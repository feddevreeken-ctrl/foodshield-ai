{"scenarios":[
"{id:\"oilseeds_canada_china_canola_tariff\", label:\"China tariffs/curbs choke Canadian canola seed exports\", cat:\"Oilseed sector\", baseSev:0.33, sevUnit:'×', channel:0,\n    desc:\"Canada is the world's #1 rapeseed/canola seed exporter; a China anti-dumping probe and retaliatory tariffs (the 2024-25 dispute) slam the door on its single largest market, leaving Chinese crushers and other canola buyers scrambling (Canola Council of Canada, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's oilseeds imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"CAN\", _src:\"Canola Council of Canada / USDA FAS\", _commodity:\"oilseeds\",\n    kick: function(c, sv=0.33) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"CAN\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.33){ return Math.round(this.kick(c, sv)); }}",

"{id:\"oilseeds_australia_canola_drought\", label:\"Drought slashes Australia's non-GM canola crop\", cat:\"Oilseed sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Australia is the world's #2 canola/rapeseed seed exporter and the EU's swing non-GM supplier; an El Niño drought across the eastern grain belt cuts the crop, tightening seed for Japanese, Belgian and French crushers (ABARES, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's oilseeds imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"AUS\", _src:\"ABARES / USDA FAS\", _commodity:\"oilseeds\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"AUS\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"oilseeds_ukraine_blacksea_war\", label:\"Black Sea war disruption cuts Ukrainian rapeseed/sunflower flows\", cat:\"Oilseed sector\", baseSev:0.32, sevUnit:'×', channel:0,\n    desc:\"Ukraine is a major rapeseed and sunflower-seed exporter; renewed strikes on Odesa-region ports and corridor uncertainty choke Black Sea seed shipments, hitting EU crushers (Germany, Belgium, Netherlands, France) that take 80%+ of its rapeseed (USDA FAS Kyiv, FAO).\",\n    formula:\"Δ = (shocked-exporter share of this country's oilseeds imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"UKR\", _src:\"USDA FAS Kyiv / FAO\", _commodity:\"oilseeds\",\n    kick: function(c, sv=0.32) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"UKR\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.32){ return Math.round(this.kick(c, sv)); }}",

"{id:\"oilseeds_russia_seed_export_tax\", label:\"Russia widens oilseed-seed export ban/tax\", cat:\"Oilseed sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Russia is the world's largest sunflower-seed producer and a major rapeseed grower; an extended seed export ban and higher export duties keep seed in domestic crush, cutting the rapeseed it still ships to China and Kazakhstan via the Far East (USDA FAS, APK-Inform).\",\n    formula:\"Δ = (shocked-exporter share of this country's oilseeds imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"RUS\", _src:\"USDA FAS / APK-Inform\", _commodity:\"oilseeds\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"RUS\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseeds (rapeseed/sunflower)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"procgrains_turkey_flour_export_curb\", label:\"Turkey curbs wheat-flour exports to protect domestic supply\", cat:\"Processed-grain sector\", baseSev:0.32, sevUnit:'×', channel:0,\n    desc:\"Turkey is the world's largest wheat-flour exporter, milling imported wheat for re-export; a licensing curb or quota to shield domestic bread supply chokes shipments to Iraq, Syria, Yemen and Sub-Saharan buyers dependent on Turkish flour (USDA FAS, Turkish Flour Industrialists' Federation).\",\n    formula:\"Δ = (shocked-exporter share of this country's procgrains imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"TUR\", _src:\"USDA FAS / Turkish Flour Industrialists' Federation\", _commodity:\"procgrains\",\n    kick: function(c, sv=0.32) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"TUR\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.32){ return Math.round(this.kick(c, sv)); }}",

"{id:\"procgrains_kazakhstan_flour_disruption\", label:\"Kazakhstan flour-export disruption hits Central Asia & Afghanistan\", cat:\"Processed-grain sector\", baseSev:0.31, sevUnit:'×', channel:0,\n    desc:\"Kazakhstan is the main wheat-flour supplier to Central Asia and Afghanistan; a rail-logistics breakdown or export restriction during a tight wheat year strands flour bound for Uzbekistan, Tajikistan, Turkmenistan and Afghan markets reliant on Kazakh mills (FAO, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's procgrains imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"KAZ\", _src:\"FAO / USDA FAS\", _commodity:\"procgrains\",\n    kick: function(c, sv=0.31) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"KAZ\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.31){ return Math.round(this.kick(c, sv)); }}",

"{id:\"procgrains_italy_durum_pasta\", label:\"Durum shortfall squeezes Italy's pasta exports\", cat:\"Processed-grain sector\", baseSev:0.29, sevUnit:'×', channel:0,\n    desc:\"Italy is the world's dominant pasta exporter; a poor southern-Italian and Mediterranean durum-wheat harvest plus costly imported durum raises mill costs and tightens semolina-pasta supply for US, German, French and UK buyers (USDA FAS, Italmopa/Unione Italiana Food).\",\n    formula:\"Δ = (shocked-exporter share of this country's procgrains imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"ITA\", _src:\"USDA FAS / Italmopa\", _commodity:\"procgrains\",\n    kick: function(c, sv=0.29) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"ITA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.29){ return Math.round(this.kick(c, sv)); }}",

"{id:\"procgrains_russia_flour_export_quota\", label:\"Russia caps flour & malt exports under grain quota\", cat:\"Processed-grain sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Russia, the world's top wheat exporter, has expanded into milled flour; extending its grain export quota and duties to flour and malt would curb the rising volumes it ships to Georgia, Central Asia, the Middle East and Africa (USDA FAS, IGC).\",\n    formula:\"Δ = (shocked-exporter share of this country's procgrains imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"RUS\", _src:\"USDA FAS / IGC\", _commodity:\"procgrains\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"RUS\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Flour, pasta & malt\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"roots_thailand_cassava_mosaic\", label:\"Cassava mosaic disease ravages Thailand's cassava crop\", cat:\"Roots sector\", baseSev:0.33, sevUnit:'×', channel:0,\n    desc:\"Thailand is the world's largest exporter of cassava products (chips, starch, pellets); a spreading cassava mosaic virus outbreak compounded by drought slashes root supply, hitting China and other Asian starch and feed buyers that depend on Thai cassava (FAO, USDA FAS, Thai Tapioca Trade Association).\",\n    formula:\"Δ = (shocked-exporter share of this country's roots imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"THA\", _src:\"FAO / Thai Tapioca Trade Association\", _commodity:\"roots\",\n    kick: function(c, sv=0.33) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"THA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.33){ return Math.round(this.kick(c, sv)); }}",

"{id:\"roots_vietnam_cassava_drought\", label:\"Drought and disease cut Vietnam's cassava exports\", cat:\"Roots sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Vietnam is a major cassava and cassava-starch exporter, second to Thailand for the Chinese market; drought in the Central Highlands plus mosaic-disease pressure reduces root and starch output, tightening supply for China and regional buyers (FAO, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's roots imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"VNM\", _src:\"FAO / USDA FAS\", _commodity:\"roots\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"VNM\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"roots_cambodia_cassava_logistics\", label:\"Logistics breakdown strands Cambodia's cassava exports\", cat:\"Roots sector\", baseSev:0.28, sevUnit:'×', channel:0,\n    desc:\"Cambodia is a fast-growing cassava exporter that ships most fresh roots and chips overland to Thailand and Vietnam for processing; a border-logistics or quality-standard disruption strands its raw cassava, squeezing the regional starch supply chain (FAO, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's roots imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"KHM\", _src:\"FAO / USDA FAS\", _commodity:\"roots\",\n    kick: function(c, sv=0.28) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"KHM\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.28){ return Math.round(this.kick(c, sv)); }}",

"{id:\"roots_nigeria_cassava_shortfall\", label:\"Domestic shortfall ends Nigeria's cassava-derivative exports\", cat:\"Roots sector\", baseSev:0.29, sevUnit:'×', channel:0,\n    desc:\"Nigeria is the world's largest cassava producer with a growing trade in starch, flour and chips; flooding and insecurity in the cassava belt cause a domestic shortfall that forces export restrictions, cutting regional African and specialty buyers off from Nigerian cassava products (FAO, IITA).\",\n    formula:\"Δ = (shocked-exporter share of this country's roots imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"NGA\", _src:\"FAO / IITA\", _commodity:\"roots\",\n    kick: function(c, sv=0.29) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"NGA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Cassava & roots\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.29){ return Math.round(this.kick(c, sv)); }}",

"{id:\"feedcake_argentina_soymeal_drought\", label:\"Drought and strikes throttle Argentina's soymeal exports\", cat:\"Feed-meal sector\", baseSev:0.33, sevUnit:'×', channel:0,\n    desc:\"Argentina is the world's #1 soybean-meal exporter; a Pampas drought slashing the crush plus an oilseed-workers' strike at Rosario-area ports chokes meal shipments, hitting EU, Southeast-Asian and North-African livestock feeders dependent on Argentine soymeal (USDA FAS, Rosario Grain Exchange/BCR).\",\n    formula:\"Δ = (shocked-exporter share of this country's feedcake imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"ARG\", _src:\"USDA FAS / Rosario Grain Exchange (BCR)\", _commodity:\"feedcake\",\n    kick: function(c, sv=0.33) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"ARG\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.33){ return Math.round(this.kick(c, sv)); }}",

"{id:\"feedcake_brazil_soymeal_logistics\", label:\"Port and road bottlenecks stall Brazil's soymeal exports\", cat:\"Feed-meal sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Brazil is a top-two soybean-meal exporter; heavy rains and truck-route bottlenecks to Santos and northern arc ports, plus a transport strike, delay meal cargoes, tightening supply for EU, Asian and Middle-East feed buyers (USDA FAS, ABIOVE).\",\n    formula:\"Δ = (shocked-exporter share of this country's feedcake imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"BRA\", _src:\"USDA FAS / ABIOVE\", _commodity:\"feedcake\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"BRA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"feedcake_usa_soymeal_export_squeeze\", label:\"Mississippi low water squeezes US soymeal exports\", cat:\"Feed-meal sector\", baseSev:0.29, sevUnit:'×', channel:0,\n    desc:\"The US is a leading soybean-meal exporter; record-low Mississippi River levels that restrict barge drafts to Gulf export elevators, combined with strong domestic crush demand, curb meal availability for Latin-American and Asian feed buyers (USDA FAS, USDA NASS).\",\n    formula:\"Δ = (shocked-exporter share of this country's feedcake imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"USA\", _src:\"USDA FAS / USDA NASS\", _commodity:\"feedcake\",\n    kick: function(c, sv=0.29) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"USA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.29){ return Math.round(this.kick(c, sv)); }}",

"{id:\"feedcake_india_meal_export_curb\", label:\"India curbs oilmeal exports amid feed-price pressure\", cat:\"Feed-meal sector\", baseSev:0.28, sevUnit:'×', channel:0,\n    desc:\"India is a significant exporter of rapeseed (mustard) and groundnut/soy oilmeal to Asian feed markets; a weak monsoon and domestic feed-inflation politics prompt export curbs, tightening supply for South-Korean, Vietnamese, Thai and other Asian buyers of Indian meal (Solvent Extractors' Association of India, USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's feedcake imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"IND\", _src:\"Solvent Extractors' Association of India (SEA) / USDA FAS\", _commodity:\"feedcake\",\n    kick: function(c, sv=0.28) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"IND\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Oilseed meal (feed)\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.28){ return Math.round(this.kick(c, sv)); }}",

"{id:\"wine_france_frost_mildew\", label:\"Frost and mildew slash France's wine output\", cat:\"Wine sector\", baseSev:0.32, sevUnit:'×', channel:0,\n    desc:\"France is one of the world's two largest wine exporters; a severe spring frost followed by downy-mildew pressure across Bordeaux, Burgundy and Champagne cuts the vintage, tightening supply and lifting prices for US, UK, German and other import markets (OIV, FranceAgriMer).\",\n    formula:\"Δ = (shocked-exporter share of this country's wine imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"FRA\", _src:\"OIV / FranceAgriMer\", _commodity:\"wine\",\n    kick: function(c, sv=0.32) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"FRA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.32){ return Math.round(this.kick(c, sv)); }}",

"{id:\"wine_italy_drought\", label:\"Drought and heat shrink Italy's wine vintage\", cat:\"Wine sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Italy vies with France as the world's top wine exporter by volume; a prolonged southern and central drought with extreme summer heat reduces the harvest, squeezing US, German, UK and Swiss buyers of Italian wine (OIV, ISTAT/Assoenologi).\",\n    formula:\"Δ = (shocked-exporter share of this country's wine imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"ITA\", _src:\"OIV / Assoenologi\", _commodity:\"wine\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"ITA\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}",

"{id:\"wine_spain_drought\", label:\"Andalusian drought cuts Spain's bulk-wine supply\", cat:\"Wine sector\", baseSev:0.29, sevUnit:'×', channel:0,\n    desc:\"Spain is the world's largest vineyard area and a leading bulk-wine exporter; a deep multi-year drought across La Mancha and the south slashes the harvest to historic lows, tightening bulk-wine supply for France, Germany, Portugal and other blending buyers (OIV, Spanish Wine Market Observatory/OeMv).\",\n    formula:\"Δ = (shocked-exporter share of this country's wine imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"ESP\", _src:\"OIV / OeMv Spain\", _commodity:\"wine\",\n    kick: function(c, sv=0.29) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"ESP\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.29){ return Math.round(this.kick(c, sv)); }}",

"{id:\"wine_chile_wildfire\", label:\"Wildfires and smoke taint Chile's wine exports\", cat:\"Wine sector\", baseSev:0.3, sevUnit:'×', channel:0,\n    desc:\"Chile is the leading New-World wine exporter from the Southern Hemisphere; summer wildfires in the central valleys destroy vineyards and smoke-taint fruit, cutting export volumes to the US, UK, Brazil, China and Japan (OIV, Wines of Chile/USDA FAS).\",\n    formula:\"Δ = (shocked-exporter share of this country's wine imports) × severity × 38; non-shocked exporters get minor relief.\",\n    _shockIso:\"CHL\", _src:\"OIV / Wines of Chile / USDA FAS\", _commodity:\"wine\",\n    kick: function(c, sv=0.3) {\n      try {\n        if (!c || !c.iso) return 0;\n        const shockIso=\"CHL\";\n        if (c.iso===shockIso) return 0;\n        const imp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'import'):null;\n        if (imp && imp.length) {\n          const tot=imp.reduce((a,p)=>a+(p.kt||0),0)||1;\n          const fromShock=imp.filter(p=>p.iso===shockIso).reduce((a,p)=>a+(p.kt||0),0);\n          return Math.pow(fromShock/tot,0.85)*sv*38;\n        }\n        const exp=(typeof curatedPartners==='function')?curatedPartners(c,\"Wine\",'export'):null;\n        if (exp && exp.length) return -Math.min(5, sv*8);\n        return 0;\n      } catch(e){ return 0; }\n    },\n    impact: function(c, sv=0.3){ return Math.round(this.kick(c, sv)); }}"
],
 "channels":[
  {"sector":"Oilseed sector","dot":"#b58a2e","blurb":"Cited bilateral oilseed-trade shocks — impact from real flow dependence."},
  {"sector":"Processed-grain sector","dot":"#c9a25a","blurb":"Cited bilateral processed-grain-trade shocks — impact from real flow dependence."},
  {"sector":"Roots sector","dot":"#9c6b3f","blurb":"Cited bilateral roots-trade shocks — impact from real flow dependence."},
  {"sector":"Feed-meal sector","dot":"#7d8a3a","blurb":"Cited bilateral feed-meal-trade shocks — impact from real flow dependence."},
  {"sector":"Wine sector","dot":"#7b2d3a","blurb":"Cited bilateral wine-trade shocks — impact from real flow dependence."}
 ]
}
