# robots.txt — FoodShield AI
# Open data dashboard, all pages crawlable.
# Block /_vercel internal endpoints (analytics, build artifacts).

User-agent: *
Allow: /
Disallow: /_vercel/
Disallow: /api/

# Be polite to slow upstream JSON fetches
Crawl-delay: 2

Sitemap: https://foodshield-ai-fv.vercel.app/sitemap.xml
