{
  "name": "foodshield-ai",
  "private": true,
  "description": "Static dashboard plus one serverless endpoint (api/ask.js) for Ask FoodShield.",
  "engines": { "node": ">=22" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
