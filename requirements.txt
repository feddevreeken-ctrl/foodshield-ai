# v22.20 — Repo-root Python dependency manifest for discoverability.
# The canonical list lives at scripts/requirements.txt — the GitHub Actions
# workflow installs from there. This top-level copy exists so external
# auditors / contributors find dependencies in the standard location.
-r scripts/requirements.txt
